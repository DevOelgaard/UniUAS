public enum AiContextKey
{
    TickValue_INT,
    TickMetaData,
    CurrentTargetGameObject,
}

public enum AiTickerMode
{
    DesiredFrameRate,
    TimeBudget,
    Unrestricted,
}

public enum PerformanceTag
{
    Lowest = 0, 
    Low = 1,
    Normal = 2,
    High = 3,
    Highest = 4,
}
